# Extra dependencies for reproducing the stable_generalist experiments.
# Install after requirements.txt in the same environment.

harmonypy>=0.0.10
scanorama>=1.7.4

# scRAW/scIB-E exact-stack compatibility.
scib-metrics>=0.5.1
jax>=0.4.28
jaxlib>=0.4.28
chex>=0.1.86
ml-dtypes>=0.3.2

# Used by several clustering/batch-correction runners.
hdbscan>=0.8.33
hnswlib>=0.8.0
python-igraph>=0.10.0
leidenalg>=0.9.0
networkx>=3.0

# Optional but needed for the scVI rows in the stable_generalist table.
scvi-tools>=1.3.0

# Exact legacy DESC/scAIDE runs require their original TensorFlow-era stack.
# If it is absent, SCRBenchmark writes explicit fallback outputs instead of
# failing the whole reproduction plan.
